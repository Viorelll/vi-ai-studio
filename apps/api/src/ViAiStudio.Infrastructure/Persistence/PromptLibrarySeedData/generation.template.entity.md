## Template: database entity or aggregate

```markdown
# Entity: `<Entity>`

**Schema:** `<schema>` * **Table:** `<snake_case_plural>` * **Module owner:** `<module>`
**Tenant-scoped:** yes/no * **Soft-deletable:** yes/no * **Audited:** yes/no

## Purpose

One paragraph on what this row represents in the business, not in the code.

## Columns

| Column | Type | Null | Default | Constraints | PII | Notes |
|---|---|---|---|---|---|---|
| `id` | `uuid` | no | `gen_random_uuid()` | PK | no | v7 UUID generated by the app |
| `tenant_id` | `uuid` | no | -- | FK -> `tenancy.tenants(id)` | no | Set by interceptor, never by client |
| `created_at_utc` | `timestamptz` | no | `now()` | -- | no | |
| `created_by` | `uuid` | yes | -- | -- | no | Null for system actions |
| `row_version` | `xid8` | no | -- | concurrency token | no | Optimistic concurrency |

## Relationships

| Relation | Cardinality | On delete | Navigation |
|---|---|---|---|
| `<Entity>` -> `<Other>` | many-to-one | `restrict` | `Entity.Other` / `Other.Entities` |

## Indexes

| Name | Columns | Type | Unique | Filter | Reason |
|---|---|---|---|---|---|
| `ix_<table>_tenant_created` | `(tenant_id, created_at_utc desc, id desc)` | btree | no | -- | Keyset pagination |

## Invariants

- MUST ... (enforced by: check constraint / domain / both)

## Query filters

- Global filter: `tenant_id = @currentTenant AND is_deleted = false`
- Escape hatch: `IgnoreQueryFilters()` permitted only in `<listed use cases>`

## Lifecycle

Created by ... Modified by ... Deleted by ... (soft/hard) Purged after ... Erasure behaviour ...

## Acceptance criteria

| # | Given | When | Then |
|---|---|---|---|
| AC-1 | tenant A row exists | tenant B queries by its id | 404, and no row returned by the repository |
```
